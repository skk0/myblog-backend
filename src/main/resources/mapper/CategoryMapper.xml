<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.skk.blog.mapper.CategoryMapper">

    <!-- 查询分类列表(含文章数) -->
    <select id="selectCategoriesWithCount" resultType="category">
        SELECT c.id, c.name, c.slug, c.icon, c.description, c.color, c.sort_order,
               COUNT(a.id) as count
        FROM category c
        LEFT JOIN article a ON c.id = a.category_id AND a.status = 'published'
        GROUP BY c.id
        ORDER BY c.sort_order ASC, c.id ASC
    </select>

</mapper>
