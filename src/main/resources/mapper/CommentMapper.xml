<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.skk.blog.mapper.CommentMapper">

    <!-- 查询评论关联的文章信息 -->
    <select id="selectCommentWithArticle" resultType="comment">
        SELECT c.*, a.slug as article_slug, a.title as article_title
        FROM comment c
        LEFT JOIN article a ON c.article_id = a.id
        WHERE c.id = #{id}
    </select>

    <!-- 统计文章评论数 -->
    <select id="countByArticleId" resultType="java.lang.Long">
        SELECT COUNT(*) FROM comment
        WHERE article_id = #{articleId} AND approved = 1
    </select>

    <!-- 统计待审核评论数 -->
    <select id="pendingCount" resultType="java.lang.Long">
        SELECT COUNT(*) FROM comment WHERE approved = 0
    </select>

</mapper>
