<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.skk.blog.mapper.TagMapper">

    <!-- 查询标签列表(含文章数) -->
    <select id="selectTagsWithCount" resultType="tag">
        SELECT t.id, t.name, t.slug, t.color, t.create_time,
               COUNT(at.article_id) as count
        FROM tag t
        LEFT JOIN article_tag at ON t.id = at.tag_id
        LEFT JOIN article a ON at.article_id = a.id AND a.status = 'published'
        GROUP BY t.id
        ORDER BY t.id ASC
    </select>

</mapper>
